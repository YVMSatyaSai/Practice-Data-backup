
git --version
git config --global user.name "sai"
git config --global user.email "kkll@gmail.com"
git config --global --list
working directory , staging area

git init -b main
git status
git init
git add code.txt
git log
git commit -m "my first commit"
cls
git commit -a -m "commit with out staging step"
git diff
git diff --staged
git add .
git rm --cached filename.txt

git clone https://..............
dir, ls
echo " some msg " >> readme.md
cat readme.md
ls -a
git branch -M main
ssh-keygen -o
git remote add origin git@github.com:............
git push -u origin main

git push origin main
git remote -v
git tag -a v1.0 -m "1st tag"
git show v1.0
git push origin v1.0

git log --pretty=oneline
git switch
git checkout -b feature1
git checkout feature1
git branch
git switch main

git switc --create feature2
git branch
git branch --all
git branch -
git branch -d feature2

git push origin feature1
git log --graph

git merge feature1
git pull origin main
git push origin main

git rebase feature3
git checkout code.............

git stash
git stash list
git stash apply

git fork
